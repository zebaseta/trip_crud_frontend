export class SeverityCounter {    
    severity: number;
    count:string;

    constructor(severity: number, count: string) {
        this.severity = severity;
        this.count = count;        
    }
}